<section class="values-block">
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.aeg"}}</h3>
    </div>
    <div class="value-bone">
        <a class="value-increase" data-type="value" title="{{localize "widzeCTX.sheet.upValue"}}">
            <i class="fas fa-plus"></i>
        </a>
        <a class="value-decrease" data-type="value" title="{{localize "widzeCTX.sheet.deValue"}}">
            <i class="fas fa-minus"></i>
        </a>"
    </div>
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.chu"}}</h3>
    </div>
    <div class="value-bone">
        <select name="chu.bone">
            {{#select valueBase.data.bone}}
            {{#each config.boneSizes as |name type|}}
             <option value="{{type}}">{{localize name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.glo"}}</h3>
    </div>
    <div class="value-bone">
        <select name="glo.bone">
            {{#select valueBase.data.bone}}
            {{#each config.boneSizes as |name type|}}
             <option value="{{type}}">{{localize name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.int"}}</h3>
    </div>
    <div class="value-bone">
        <select name="int.bone">
            {{#select valueBase.data.bone}}
            {{#each config.boneSizes as |name type|}}
             <option value="{{type}}">{{localize name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.rev"}}</h3>
    </div>
    <div class="value-bone">
        <select name="rev.bone">
            {{#select valueBase.data.bone}}
            {{#each config.boneSizes as |name type|}}
             <option value="{{type}}">{{localize name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
    <div class="value-name">
        <h3>{{localize "widzeCTX.values.won"}}</h3>
    </div>
    <div class="value-bone">
        <select name="won.bone">
            {{#select valueBase.data.bone}}
            {{#each config.boneSizes as |name type|}}
             <option value="{{type}}">{{localize name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
</section>

